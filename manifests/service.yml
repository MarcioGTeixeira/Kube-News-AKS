apiVersion: v1
kind: Service
metadata:
    name: "myakscluster-b85a"
    labels:
        app: "myakscluster-b85a"
spec:
    type: LoadBalancer
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "myakscluster-b85a"